On the TODO list
Choose the most popular range from 2-10 instead of keeping it at 3
Fix the power button so it actually works
Fix the reset button so it actually works
Donate reroutes to portfolio entry with suggested features and donate button saying donate to suggest features
Actually publish this now

Unfiltered
Make emojis show up in the streampop dictionary

started at 3:07 on Saturday
DONE

Ignore case when agregating words
Refactors common words into an array that gets filtered out
Make the common word filtering a checkbox
